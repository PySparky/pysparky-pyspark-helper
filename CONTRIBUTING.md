
## Contribution Guidelines

When writing docstrings for functions, please adhere to the following standard:

- Use `Example:` (singular) for the examples section. This will be rendered as an admonition in the documentation.
- Do NOT use `Examples:` (plural).
